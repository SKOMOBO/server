// making a http server instead cus everything else too hard

// impact shouldnt be huge not preferable but extra abstraction is better than performance


class ClientWeb {
    public function new() {
        
    }
}
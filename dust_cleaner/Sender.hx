import haxe.remoting.LocalConnection;
class Sender
{
    static var lc    : LocalConnection;
    
    static function main()
    {

        // lc = new LocalConnection({target="_canal"});
        // // lc = LocalConnection.connect(  );
        // lc.setErrorHandler(function ( e ) { trace ( "error : " + e ); });
        // lc.Receiver.myFunction.call( [ "Test" ] );
    }
}
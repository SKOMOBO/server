package;

// haxe may have a zip file lib for combining data

// other alternative is to just join the json arrays then convert to csv instead
class file_manager {
    public function new() {
        
    }
}
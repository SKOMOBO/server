package;

// just wrap existing nodejs code, make wrapper function inside lib
// this makes it easy to make conditional for porting to other systems later
// if necessary

// would need to make wrapper objects for resp and req as well probably
class router {
    public function new() {
        
    }
}
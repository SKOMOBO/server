var box_schema = {
    'ID':0,
    'Box_ID':0,
    'Temperature':0.0,
    'Humidity':0.0,
    'Dust1':0,
    'Dust2_5':0,
    'Dust10':0
}

var data = {
    'box_info':{
        'ID':[],
        'processor':[]
    },
    'box1':[],
    'box2':[]
}

function query(){
    // parse things like select, where etc convert to lower case 
    // support columns

    // make function to retrieve rows from records with given column
    // make function to insert as well
}

function create_record(Box_ID, Temperature, Humidity, Dust1, Dust10, Dust2_5){
    data['']
}